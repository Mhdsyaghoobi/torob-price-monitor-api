# torob-price-monitor-api
A lightweight Python tool that uses the Torob API(via the `torob-client` wrapper) to search for products, extract price and seller information, and generate clean Excel reports. The script focuses on fetching the cheapest available prices and presenting the data in a sorted, easy‑to‑read format.
